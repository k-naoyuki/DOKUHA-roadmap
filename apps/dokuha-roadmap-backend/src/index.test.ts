import { describe, beforeAll, afterAll, it, expect } from "vitest";

describe("GET /", () => {
	

	// it("should return Hello Hono! for the root path", async () => {
  //   if (!worker) throw new Error("Worker did not initialize"); // workerの存在確認
  //   const resp = await worker.fetch("/");
  //   expect(resp.status).toBe(200);
  //   const text = await resp.text();
  //   expect(text).toBe("Hello Hono!"); // src/index.ts で定義されているレスポンス
  // });
});
